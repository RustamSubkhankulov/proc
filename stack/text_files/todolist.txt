stack flush 

stack summ

вывод стека красивый и аккуратный

done добавить андрес структуры в стек при дебаге, конструктор

done канарейки  hex_speak

done в вызове валидатора выставить проверкуБ если не прошла то дамп

doneфункции для стека: 
    doneсортировка
    выброс суммы всех эелементов
    stack_reset
 
в зависимости от имени вызванной функции писать в репорт что только что произошло

хэш структуры и хэш даты отдельные

stack_hash_check



done //обработка ошибок в валидаторе

надо ли? //проверка file_name, func_name, line на верность

done //условная компиляция на вызовы в отдельности простого и особенного

done //returning value

надо ли? //заменить постоянный set stack error code на макрос без указателя на стэк

done//stack check вызывающий сначала проверку указателя, а затем самого стека

done//stackresize заполнение элементов ядом

done//индикатор-указатель как показатель того, что стек 1)конструирован 2)использовался 

done//проверка на вызов конструктора перед другими стековыми функциями

done//обернуть функцию обработки ошибки в макрос

done//проверка указателя на null после конструктора

done//в стекпуш проверка на poison указатель

done//проверки capacity >0, size <= capacity

//typeif, typeinfo

done//рядом с elem_t сделать строковую константу с именем типа

              //generic

              //stack dump 

done//проверка в stackctor на чистоту области памяти 

done//define на логи " "

done//доп функции проверки для отдельных стековых функций

done//дополнить коды ошибок

done//функции возвращают некорректное 

done//функция котора записывает код ошибки в структуру и пишет сообщение в логи если нужно

done//в начале контруктора ставить проверку на верность структуры ( пустая или "отравленная")

          //обернуть аллокацию в макросы, массив указателей на аллоцированые участки памяти

          //инициализация по умолчанию в  stack_pop

